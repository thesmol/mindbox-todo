import { Box } from "@mui/material";

const TodoSummary = () => {
  return (
    <Box className="relative w-full paper p-4">
      <p>Контент вашего контейнера здесь</p>
    </Box>
  );
};

export default TodoSummary;
